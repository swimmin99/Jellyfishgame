using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class inputSenderSC : MonoBehaviour
{
    public GameObject keyboardInputPrefab;
    // Start is called before the first frame update
    private inputUISC inputComponenet;


    public void callInputString()
    {
       // Instantiate(keyboardInputPrefab).GetComponent<inputUISC>().inputStringFunc(ref jellName);
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
